const { expect } = require('chai');

describe('Aula', () => {
  it('Aulas ainda não foram implementadas', (done) => {
    // Placeholder para testes de aula
    // A ser implementado conforme novos requisitos
    expect(true).to.equal(true);
    done();
  });
});
